{
  "name": "voz-notas",
  "version": "0.0.1",
  "description": "Talk to your notes by voice.",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/qrcode": "^1.5.6",
    "esbuild": "latest",
    "obsidian": "latest",
    "typescript": "~6.0.2"
  },
  "dependencies": {
    "@noble/ciphers": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "qrcode": "^1.5.4"
  }
}
